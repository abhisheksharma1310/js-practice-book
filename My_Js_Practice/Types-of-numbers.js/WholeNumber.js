/**
 * Whole Numbers
 * Counting numbers together with zero  0,1,2,3 ... are called whole numbers.
 */

 function wholeNumber(count = 100) {
    for(let num = 0; num < count; num++){
        console.log(num);
    };
};

wholeNumber(100);

export default wholeNumber;